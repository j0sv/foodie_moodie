Restaurant: <%= @restaurant.name %>


<% @dishes.each do |dish| %>
    <%= dish.title %>
    <%= dish.description %>
    <%= dish.price %>

    <%= form_tag(orders_path) do %>
      <%= submit_tag("Add", id: dish.id) do %>

      <% end %>
      <%= hidden_field_tag :dish_id, dish.id %>
    <% end %>
<% end %>
